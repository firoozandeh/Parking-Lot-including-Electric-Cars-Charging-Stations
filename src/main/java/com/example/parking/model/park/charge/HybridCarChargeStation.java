package com.example.parking.model.park.charge;

public class HybridCarChargeStation extends ChargeStation{
    public HybridCarChargeStation(String id, ChargeType chargeType) {
        super(id, ChargeType.HYBRID_CAR);
    }
}
